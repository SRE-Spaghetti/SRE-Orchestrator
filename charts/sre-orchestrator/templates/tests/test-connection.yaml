apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sre-orchestrator.fullname" . }}-test-connection"
  labels:
    {{- include "sre-orchestrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sre-orchestrator.fullname" . }}:{{ .Values.orchestrator.service.port }}']
  restartPolicy: Never
