{{- if .Values.orchestrator.geminiApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sre-orchestrator.fullname" . }}-gemini-api-key
  labels:
    {{- include "sre-orchestrator.labels" . | nindent 4 }}
type: Opaque
data:
  GEMINI_API_KEY: {{ .Values.orchestrator.geminiApiKey | b64enc | quote }}
{{- end }}
